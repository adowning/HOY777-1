import routes from './routes';
import * as types from './types';

/**
 * Hono routes for the VipLevel model
 */
export const vipLevelRoutes = routes;

/**
 * Types for the VipLevel model
 */
export const vipLevelTypes = types;

export default routes;
