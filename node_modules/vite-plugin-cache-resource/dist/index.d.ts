import { Plugin } from 'vite';

interface Options {
    /**
     * where to save cached file
     * @default `${process.cwd()}/tmp`
     */
    cacheDir?: string;
}
declare const cacheResource: (userOptions?: Options) => Plugin;

export { cacheResource };
